# Cloudflare Pages 构建设置
# 此文件用于 Git CI/CD 部署配置

build_command: npm run build:cf
build_output_dir: .open-next/worker

# 环境变量（可选，在 Dashboard 中配置更安全）
# environment_variables:
#   NEXT_PUBLIC_APP_URL: "https://your-project.pages.dev"
